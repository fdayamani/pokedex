dependencies {
    implementation libs.bundles.app
    testImplementation libs.bundles.appTest
}

test {
    useJUnitPlatform()
}

bootJar {
    archiveBaseName = 'pokedex-app'
    archiveVersion = ''
}

jar {
    enabled = false
}
